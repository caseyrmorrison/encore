/** The festival-season stages, split into their own chunk (see tour.ts). */
export { Desert } from '../render/venues/desert';
export { Fields } from '../render/venues/fields';
export { Megafest } from '../render/venues/megafest';
